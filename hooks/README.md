# hooks/

라이프사이클 자동화 훅. `hooks.json`을 이 디렉토리에 두면 Claude Code v2.1+가 컨벤션으로
자동 로드합니다 (`.claude-plugin/plugin.json`에 `hooks` 필드를 명시적으로 추가하면 안 됨 —
"Duplicate hooks file detected" 오류 발생).

아직 등록된 훅이 없습니다.
