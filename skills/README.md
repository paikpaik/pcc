# skills/

재사용 가능한 워크플로우/도메인 지식을 담습니다. 각 스킬은 자체 디렉토리를 가집니다
(`skills/<skill-name>/SKILL.md` 형태 권장).

권장 섹션 구성:

- When to Use
- How It Works
- Examples

실제 작업에서 반복되는 패턴이 보이면 하나씩 추가합니다. 등록된 스킬은 `docs/skills.html` 참고.
